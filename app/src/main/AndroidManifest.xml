<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  package="id.junkman">

  <uses-permission android:name="android.permission.INTERNET" />

  <application
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/Theme.JunkMan">
    <activity android:name=".ui.shoppingcart.ShoppingCartActivity">
      <meta-data
        android:name="android.support.PARENT_ACTIVITY"
        android:value=".ui.LandingActivity" />
    </activity>
    <activity android:name=".ui.profile.withdraw.WithdrawActivity" />
    <activity android:name=".ui.scan.OrderActivity" />
    <activity android:name=".ui.profile.historytrans.HistoryTransActivity" />
    <activity android:name=".ui.profile.editprofile.EditProfileActivity" />
    <activity android:name=".ui.transaction.TransactionActivity">
      <meta-data
        android:name="android.support.PARENT_ACTIVITY"
        android:value=".ui.LandingActivity" />
    </activity>
    <activity android:name=".ui.WelcomeActivity" />
    <activity android:name=".ui.LoginActivity" />
    <activity android:name=".ui.LandingActivity" />
    <activity
      android:name=".ui.SplashActivity"
      android:theme="@style/Theme.JunkMan.NoActionBar">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>

    <meta-data
      android:name="com.google.android.gms.version"
      android:value="@integer/google_play_services_version" />
    <meta-data
      android:name="com.google.android.geo.API_KEY"
      android:value="@string/google_maps_key" />
  </application>

</manifest>